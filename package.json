{
  "name": "skill",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:browse": "bun build --compile --external chromium-bidi --external electron ./skills/browse/src/cli.ts --outfile ./dist/browse && bun build --compile --external chromium-bidi --external electron ./skills/browse/src/server.ts --outfile ./dist/browse-server"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "playwright": "^1.58.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "bun-types": "^1.3.13"
  }
}
